# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# environment variables and secrets: local.env holds NOTION_TOKEN
*.env
.env.*

# macOS
.DS_Store

# jetbrains
.idea/

# sync --dry-run output, for inspecting before it lands in src/content/
preview/

# the WordPress export the migration reads. Big, and a snapshot of a source of
# truth we are deliberately retiring — keeping it committed would invite
# somebody to treat it as one. See MIGRATION.md.
wordpress-export/
